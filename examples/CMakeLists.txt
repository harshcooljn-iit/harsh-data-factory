# ---------------------------------------------------------------------------
# Example helper programs.
#
# Each example pipeline under examples/<name>/ ships a pipeline.json plus any
# tiny deterministic helper programs it needs. Native helpers are built here so
# the examples run from a clean checkout without external tools. Targets are
# added as the example pipelines land.
# ---------------------------------------------------------------------------
